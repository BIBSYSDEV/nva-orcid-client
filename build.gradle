plugins{
    id 'nva.orcid.client.rootplugin'
}

//workaround for jacoco-merge to work
allprojects{
    apply plugin: 'nva.orcid.client.java-conventions'
    apply plugin: 'nva.orcid.client.dynamo-db'
}

wrapper {
    gradleVersion = '8.12'
    distributionType = Wrapper.DistributionType.ALL
}